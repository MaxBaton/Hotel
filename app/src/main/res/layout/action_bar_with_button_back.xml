<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="?attr/actionBarSize"
    xmlns:tools="http://schemas.android.com/tools"
    android:background="@color/fragment_background_main"
    android:id="@+id/layout">

    <ImageView
        android:id="@+id/image_view_back"
        android:layout_width="@dimen/icon_arrow_back_sizes"
        android:layout_height="@dimen/icon_arrow_back_sizes"
        android:src="@drawable/icon_arrow_back"
        android:scaleType="centerCrop"
        android:adjustViewBounds="true"
        android:layout_marginStart="@dimen/hotel_fragment_margin_start_end_elements"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"/>

    <TextView
        android:id="@+id/text_view_title"
        style="@style/Hotel_ActionBarTextView"
        tools:text="Title"
        android:layout_marginEnd="@dimen/hotel_fragment_margin_start_end_elements"
        app:layout_constraintStart_toEndOf="@id/image_view_back"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        />

</androidx.constraintlayout.widget.ConstraintLayout>